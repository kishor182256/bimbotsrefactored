export const drawingMode = {
  LINE: 'LINE',
};

export const defaultLineOptions = {
  strokeWidth: 2,
  stroke: 'red',
  fill: 'red',
  strokeUniform: true,
  noScaleCache: false,
};

export const ZOOM_STEP = 0.2;
